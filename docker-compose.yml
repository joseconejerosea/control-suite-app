services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: controlsuite-backend-${ENV:-dev}
    env_file: ./backend/.env
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    restart: always
