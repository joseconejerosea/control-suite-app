/**
 * StartOnboardingDto — placeholder for future use.
 *
 * Step 1 (client_created) is handled directly by ClientsController.
 * This DTO is included in the module structure per the M4 specification
 * and can be extended if a dedicated /start endpoint is added.
 */
export class StartOnboardingDto {}